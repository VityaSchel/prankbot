import Cookies from"js-cookie";let status={wasVerified:false,previousResult:null};export const hasCheckboxes=companyActive=>{if(!status.wasVerified){if(Cookies.get("visited")===undefined){const params=new URLSearchParams(window.location.search);if(params.has("ads")&&companyActive){Cookies.set("visited",String(Date.now()),{expires:365});const params=new URLSearchParams(window.location.search);window.history.pushState({},"",window.location.pathname+(params.toString()&&"?"+params.toString()));status={wasVerified:true,previousResult:false};return false}else{status={wasVerified:true,previousResult:false};return true}}else{status={wasVerified:true,previousResult:true};return true}}else{return status.previousResult}};export const resetVerificationStatus=()=>{status.wasVerified=false};